name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
      - name: Install
        run: |
          uv sync --extra dev
      - name: Pip Editable Install Compatibility
        run: |
          uv pip install -e .
      - name: Private File Policy
        run: ./scripts/check_blocked_paths.sh
      - name: Lint
        run: uv run ruff check .
      - name: Typecheck
        run: uv run mypy src
      - name: Test
        run: uv run pytest -q
      - name: Determinism Replay Tests
        run: uv run pytest -q tests/integration/test_determinism_replay.py
      - name: Coverage
        run: uv run pytest --cov=src/trajectly --cov-report=term-missing -q
      - name: Smoke
        run: |
          uv run trajectly init
          uv run trajectly record tests/*.agent.yaml
          uv run trajectly run tests/*.agent.yaml
