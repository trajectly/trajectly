name: example-real-gemini
command: python examples/real_llm_ci/runner.py
workdir: ..
fixture_policy: by_hash
strict: true
env:
  REAL_PROVIDER: gemini
  REAL_MODE: baseline
  REAL_MODEL: gemini-2.5-flash
contracts:
  tools:
    allow: [fetch_ticket, store_triage]
    deny: [publish_raw_pii]
