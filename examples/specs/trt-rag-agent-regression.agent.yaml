schema_version: "0.3"
name: "trt-rag-agent"
command: "python -m examples.rag_agent.main_regression"
workdir: ..
fixture_policy: by_hash
strict: true
contracts:
  tools:
    allow: [query_index, rerank, format_answer]
    deny: [unsafe_export]
